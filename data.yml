defaultStorageMapping: DATA_GENERATOR_LOC
jobs: {}
locations:
  COALESCE_DEV:
    mappingDefinitions:
      "3":
        database: TESTING_DATA_INGESTION
        schema: PUBLIC
  DATA_GENERATOR_LOC:
    mappingDefinitions:
      "3":
        database: DATA_GENERATOR_DB
        schema: DATA_GENERATOR_SCHEMA
  IMS_MIDAS_LOC:
    mappingDefinitions:
      "3":
        database: IMS_MIDAS
        schema: LANDING
macros: {}
packages: {}
projects: {}
stepTypes:
  StepType-2:
    id: "2"
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |
        capitalized: NumberValueGenerator
        short: NVG
        plural: NumberValueGenerators
        tagColor: '#00ffff'

        config:
        - groupName: Options
          items:
          - displayName: Minimum Value
            attributeName: minValue
            type: textBox
            isRequired: true
            
          - displayName: Maximum Value
            attributeName: maxValue
            type: textBox
            isRequired: true
            
          - displayName: Distribution
            attributeName: distribution
            type: dropdownSelector
            options:
            - 'Uniform'
            - 'Normal'
            - 'Zipf'
            isRequired: true

          - displayName: Truncate Before
            attributeName: truncateBefore
            type: toggleButton
            default: true

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false
      templates:
        create:
          templateString: "\t{% set minValParts = config.minValue.split('.') %}\r

            \t{% set maxValParts = config.maxValue.split('.') %}\r

            \t{% set valNumLen =
            ([minValParts[0]|length,maxValParts[0]|length]|max) %}\r

            \t{% set valDecLen =
            ([minValParts[1]|length,maxValParts[1]|length]|max) %}\r

            \r

            \t{{ stage('Create Stage Table') }}\r

            \r

            \tCREATE OR REPLACE TABLE {{
            ref_no_link(node.location.name, node.name) }}\r

            \t(\r

            \t\t{% for col in columns %}\r

            \t\t\t\"{{ col.name }}\" {{ col.dataType }}\r

            \t\t\t{%- if not col.nullable %} NOT NULL\r

            \t\t\t\t{%- if col.defaultValue | length > 0 %} DEFAULT
            {{ col.defaultValue }}{% endif %}\r

            \t\t\t{% endif %}\r

            \t\t\t{%- if col.description | length > 0 %} COMMENT '{{
            col.description }}'{% endif %}\r

            \t\t\t{%- if not loop.last -%}, {% endif %}\r

            \t\t{% endfor %}, \r

            \t\t\"NUMBERVALUE\"
            NUMBER({{valNumLen+valDecLen}},{{valDecLen}})\r

            \t)\r

            \t{%- if node.description | length > 0 %} COMMENT = '{{
            node.description }}'{% endif %}\r\n"
        run:
          templateString: "\r

            \    {% for test in node.tests if config.testsEnabled
            %}\r

            \        {% if test.runOrder == 'Before' %}\r

            \            {{ test_stage(test.name,
            test.continueOnFailure) }}\r

            \            {{ test.templateString }}\r

            \        {% endif %}\r

            \    {% endfor %}\r

            \r

            {% if config.preSQL %}\r

            \t{{ stage('Pre-SQL') }}\r

            \t{{ config.preSQL }}\r

            {% endif %}\r

            \r

            \t{% if config.truncateBefore %}\r

            \r

            \t\t{{ stage('Truncate Stage Table') }}\r

            \t\tTRUNCATE IF EXISTS {{
            ref_no_link(node.location.name, node.name) }}\r

            \r

            \t{% endif %}\r

            \r

            \t{% for source in sources %}\r

            \r

            \t\t{{ stage('Insert ' + source.name | string ) }}\r

            \r

            \t\tINSERT INTO {{ ref_no_link(node.location.name,
            node.name) }}\r

            \t\t(\r

            \t\t\t{% for col in source.columns %}\r

            \t\t\t\t\"{{ col.name }}\"\r

            \t\t\t\t{%- if not loop.last -%},{% endif %}\r

            \t\t\t{% endfor %},\r

            \t\t\t\"NUMBERVALUE\"\r

            \t\t)\r

            \r

            \t\tSELECT\r

            \t\t{% for col in source.columns %}\r

            \               {{ get_source_transform(col) }} AS \"{{
            col.name }}\"\r

            \t\t\t{%- if not loop.last -%}, {% endif %}\r

            \t\t{% endfor %},\r

            \t\t\t{{config.distribution}}({{config.minValue}},{{con\
            fig.maxValue}},RANDOM())\r

            \r

            \t\t{{ source.join }}\r

            \r

            \t{% endfor %}\r

            \r

            {% if config.postSQL %}\r

            \t{{ stage('Post-SQL') }}\r

            \t{{ config.postSQL }}\r

            {% endif %}\r

            \r

            {% if config.testsEnabled %}\r

            \t{% for test in node.tests %}\r

            \t\t{% if test.runOrder == 'After' %}\r

            \t\t\t{{ test_stage(test.name, test.continueOnFailure)
            }}\r

            \t\t\t{{ test.templateString }}\r

            \        {% endif %}\r

            \t{% endfor %}\r

            \r

            \t{% for column in columns %}\r

            \t\t{% for test in column.tests %}\r

            \t\t\t{{ test_stage(column.name + \": \" + test.name)
            }}\r

            \t\t\t{{ test.templateString }}\r

            \t\t{% endfor %}\r

            \t{% endfor %}\r

            {% endif %}\r\n"
    name: NumberValueGenerator
    version: 1
  StepType-4:
    id: "4"
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |
        capitalized: Copy of NumberValueGenerator
        short: NVG
        plural: NumberValueGenerators
        tagColor: '#00ffff'

        config:
        - groupName: Options
          items:
          - displayName: Minimum Value
            attributeName: minValue
            type: textBox
            isRequired: true
            
          - displayName: Maximum Value
            attributeName: maxValue
            type: textBox
            isRequired: true
            
          - displayName: Distribution
            attributeName: distribution
            type: dropdownSelector
            options:
            - 'Uniform'
            - 'Normal'
            - 'Zipf'
            isRequired: true

          - displayName: Truncate Before
            attributeName: truncateBefore
            type: toggleButton
            default: true

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false
      templates:
        create:
          templateString: "\t{{ stage('Create Stage Table') }}\r

            \r

            \tCREATE OR REPLACE TABLE {{
            ref_no_link(node.location.name, node.name) }}\r

            \t(\r

            \t\t{% for col in columns %}\r

            \t\t\t\"{{ col.name }}\" {{ col.dataType }}\r

            \t\t\t{%- if not col.nullable %} NOT NULL\r

            \t\t\t\t{%- if col.defaultValue | length > 0 %} DEFAULT
            {{ col.defaultValue }}{% endif %}\r

            \t\t\t{% endif %}\r

            \t\t\t{%- if col.description | length > 0 %} COMMENT '{{
            col.description }}'{% endif %}\r

            \t\t\t{%- if not loop.last -%}, {% endif %}\r

            \t\t{% endfor %}, \r

            \t\t\"NUMBERVALUE\" INTEGER\r

            \t)\r

            \t{%- if node.description | length > 0 %} COMMENT = '{{
            node.description }}'{% endif %}\r\n"
        run:
          templateString: "\r

            \    {% for test in node.tests if config.testsEnabled
            %}\r

            \        {% if test.runOrder == 'Before' %}\r

            \            {{ test_stage(test.name,
            test.continueOnFailure) }}\r

            \            {{ test.templateString }}\r

            \        {% endif %}\r

            \    {% endfor %}\r

            \r

            {% if config.preSQL %}\r

            \t{{ stage('Pre-SQL') }}\r

            \t{{ config.preSQL }}\r

            {% endif %}\r

            \r

            \t{% if config.truncateBefore %}\r

            \r

            \t\t{{ stage('Truncate Stage Table') }}\r

            \t\tTRUNCATE IF EXISTS {{
            ref_no_link(node.location.name, node.name) }}\r

            \r

            \t{% endif %}\r

            \r

            \t{% for source in sources %}\r

            \r

            \t\t{{ stage('Insert ' + source.name | string ) }}\r

            \r

            \t\tINSERT INTO {{ ref_no_link(node.location.name,
            node.name) }}\r

            \t\t(\r

            \t\t\t{% for col in source.columns %}\r

            \t\t\t\t\"{{ col.name }}\"\r

            \t\t\t\t{%- if not loop.last -%},{% endif %}\r

            \t\t\t{% endfor %},\r

            \t\t\t\"NUMBERVALUE\"\r

            \t\t)\r

            \r

            \t\tSELECT\r

            \t\t{% for col in source.columns %}\r

            \               {{ get_source_transform(col) }} AS \"{{
            col.name }}\"\r

            \t\t\t{%- if not loop.last -%}, {% endif %}\r

            \t\t{% endfor %},\r

            \t\t\t{{config.distribution}}({{config.minValue}},{{con\
            fig.maxValue}},RANDOM())\r

            \r

            \t\t{{ source.join }}\r

            \r

            \t{% endfor %}\r

            \r

            {% if config.postSQL %}\r

            \t{{ stage('Post-SQL') }}\r

            \t{{ config.postSQL }}\r

            {% endif %}\r

            \r

            {% if config.testsEnabled %}\r

            \t{% for test in node.tests %}\r

            \t\t{% if test.runOrder == 'After' %}\r

            \t\t\t{{ test_stage(test.name, test.continueOnFailure)
            }}\r

            \t\t\t{{ test.templateString }}\r

            \        {% endif %}\r

            \t{% endfor %}\r

            \r

            \t{% for column in columns %}\r

            \t\t{% for test in column.tests %}\r

            \t\t\t{{ test_stage(column.name + \": \" + test.name)
            }}\r

            \t\t\t{{ test.templateString }}\r

            \t\t{% endfor %}\r

            \t{% endfor %}\r

            {% endif %}\r\n"
    name: IntegerValueGenerator
    version: 1
  StepType-Dimension:
    id: Dimension
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |-
        capitalized: Dimension
        short: DIM
        tagColor: '#1E339A'
        plural: Dimensions

        config:
        - groupName: Options
          items:
          - type: materializationSelector
            isRequired: true
            default: table
            options:
            - table
            - view

          - type: multisourceToggle
            enableIf: "{% if node.materializationType == 'table' %} true {% else %} false {% endif %}"

          - type: businessKeyColumns
            isRequired: true

          - type: changeTrackingColumns
            isRequired: false

          - type: overrideSQLToggle
            enableIf: "{% if node.materializationType == 'view' %} true {% else %} false {% endif %}"

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false

        systemColumns:
        - displayName: '{{NODE_NAME}}_KEY'
          transform: ''
          dataType: NUMBER
          placement: beginning
          attributeName: isSurrogateKey

        - displayName: SYSTEM_VERSION
          transform: ''
          dataType: NUMBER
          placement: end
          attributeName: isSystemVersion

        - displayName: SYSTEM_CURRENT_FLAG
          transform: ''
          dataType: VARCHAR
          placement: end
          attributeName: isSystemCurrentFlag

        - displayName: SYSTEM_START_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemStartDate

        - displayName: SYSTEM_END_DATE
          transform: CAST('2999-12-31 00:00:00' AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemEndDate

        - displayName: SYSTEM_CREATE_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemCreateDate

        - displayName: SYSTEM_UPDATE_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemUpdateDate
      templates:
        create:
          templateString: |
            {% if node.materializationType == 'table' %}
            	{{ stage('Create Dimension Table') }}

            	CREATE OR REPLACE TABLE {{ ref_no_link(node.location.name, node.name) }}
            	(
            		{% for col in columns %}
            			"{{ col.name }}" {{ col.dataType }}
            			{% if col.isSurrogateKey %}
            		        identity
            			{% endif %}
            			{%- if not col.nullable %} NOT NULL
            				{%- if col.defaultValue | length > 0 %} DEFAULT {{ col.defaultValue }}{% endif %}
            			{% endif %}
            			{%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
            			{%- if not loop.last -%}, {% endif %}
            		{% endfor %}
            	)
            	{%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}


            {% elif node.materializationType == 'view' %}
            	{{ stage('Create Dimension View') }}

            	CREATE OR REPLACE VIEW {{ ref_no_link(node.location.name, node.name) }}
            	(
            		{% for col in columns %}
            			"{{ col.name }}"
            			{%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
            			{%- if not loop.last -%},{% endif %}
            		{% endfor %}
            	)
            	{%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}
            	AS
            	{% for source in sources %}

            		{% if loop.first %}SELECT {% endif %}

            		{% for col in source.columns %}
            			{% if col.isSurrogateKey or col.isSystemUpdateDate or col.isSystemCreateDate %}
                            NULL
            			{% else %}
                            {{ get_source_transform(col) }}
            			{% endif %}
            			AS "{{ col.name }}"
            			{%- if not loop.last -%}, {% endif %}
            		{% endfor %}
            		{{ source.join }}

            		{% if not loop.last %} UNION ALL {% endif %}
            	{% endfor %}

            {% endif %}
        run:
          templateString: |
            
            {% set is_type_2 = columns | selectattr("isChangeTracking") | list | length > 0 %}

                {% for test in node.tests if config.testsEnabled %}
                    {% if test.runOrder == 'Before' %}
                        {{ test_stage(test.name, test.continueOnFailure) }}
                        {{ test.templateString }}
                    {% endif %}
                {% endfor %}

            {% if node.materializationType == 'table' %}

            	{% if config.preSQL %}			
            		{{ stage('Pre-SQL') }}
            		{{ config.preSQL }}
            	{% endif %}
            	
                {% if is_type_2 %}

                    {% for source in sources %}
                        {{ stage('MERGE ' + source.name | string) }}
                        MERGE INTO {{ ref_no_link(node.location.name, node.name) }} "TGT"
                        USING (
                        /* New Rows That Don't Exist */
                        SELECT
                        {% for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemVersion %}
                                1
                            {% elif col.isSystemCurrentFlag %}
                                'Y'
                            {% else %}
                               {{ get_source_transform(col) }}
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor %}
                            'INSERT_INITAL_VERSION_ROWS' AS "DML_OPERATION"
                        {{ source.join }}
                        LEFT JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                                {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            "DIM"."{{ col.name }}" IS NULL
                        {% endfor %}
                        UNION ALL
                        /* New Row Needing To Be Inserted Due To Type-2 Column Changes */
                        SELECT
                        {% for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemVersion %}
                                "DIM"."{{ col.name }}" + 1
                            {% elif col.isSystemCurrentFlag %}
                                'Y'
                            {% else %}
                               {{ get_source_transform(col) }}
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor %}
                            'INSERT_NEW_VERSION_ROWS' AS "DML_OPERATION"
                        {{ source.join }}
                        INNER JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE "DIM"."{{ get_value_by_column_attribute("isSystemCurrentFlag") }}" = 'Y'
                        {% for col in source.columns if (col.isChangeTracking == true) %}
                            {% if loop.first %}
                                AND (
                            {% else %}
                                OR
                            {% endif %}
                            ( NVL( CAST({{ get_source_transform(col) }} as STRING), '**NULL**') <> NVL( CAST("DIM"."{{ col.name }}" as STRING), '**NULL**') )
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                        UNION ALL
                        /* Rows Needing To Be Expired Due To Type-2 Column Changes
                        In this case, only two columns are merged (version and end date) */
                        SELECT
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemEndDate %}
                                DATEADD(MILLISECONDS, -1, CAST(CURRENT_TIMESTAMP AS TIMESTAMP))
                            {% elif col.isSystemCurrentFlag %}
                                'N'
                            {% else %}
                                "DIM"."{{ col.name }}"
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor -%}
                            'update_expired_version_rows' AS "DML_OPERATION"
                        {{ source.join }}
                        INNER JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE "DIM"."{{ get_value_by_column_attribute("isSystemCurrentFlag") }}" = 'Y'
                        {% for col in source.columns if (col.isChangeTracking == true) %}
                            {% if loop.first %}
                                AND (
                            {% else %}
                                OR
                            {% endif %}
                            ( NVL( CAST({{ get_source_transform(col) }} as STRING), '**NULL**') <> NVL( CAST("DIM"."{{ col.name }}" as STRING), '**NULL**') )
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                        UNION ALL
                        /* Rows Needing To Be Updated Due To Changes To Non-Type-2 source.columns
                        This case merges only when there are changes in non-type-2 column updates, but no changes in type-2 columns*/
                        SELECT
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemVersion or col.isSystemCreateDate or col.isSystemStartDate or col.isSystemEndDate %}
                                "DIM"."{{ col.name }}"
                            {% elif col.isSystemCurrentFlag %}
                                'Y'
                            {% else %}
                                {{ get_source_transform(col) }}
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor -%}
                            'UPDATE_NON_TYPE2_ROWS' AS "DML_OPERATION"
                        {{ source.join }}
                        INNER JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE "DIM"."{{ get_value_by_column_attribute("isSystemCurrentFlag") }}" = 'Y'
                        AND (
                        {% for col in source.columns if (col.isChangeTracking) -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %} )
                        {% for col in source.columns if not (   col.isBusinessKey or
                                                                col.isChangeTracking or
                                                                col.isSurrogateKey or
                                                                col.isSystemVersion or
                                                                col.isSystemCurrentFlag or
                                                                col.isSystemStartDate or
                                                                col.isSystemEndDate or
                                                                col.isSystemUpdateDate or
                                                                col.isSystemCreateDate) -%}
                            {% if loop.first %}
                                AND (
                            {% endif %}
                            {% if not loop.first %}
                                OR
                            {% endif %}
                            NVL( CAST({{ get_source_transform(col) }} as STRING), '**NULL**') <> NVL( CAST("DIM"."{{ col.name }}" as STRING), '**NULL**')
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                    ) AS "SRC"
                    ON
                    {% for col in source.columns if col.isBusinessKey -%}
                        {% if not loop.first %}
                            AND
                        {% endif %}
                        "TGT"."{{ col.name }}" = "SRC"."{{ col.name }}"
                    {% endfor %}
                    AND "TGT"."{{ get_value_by_column_attribute("isSystemVersion") }}" = "SRC"."{{ get_value_by_column_attribute("isSystemVersion") }}"
                    WHEN MATCHED THEN UPDATE SET
                    {%- for col in source.columns if not (col.isBusinessKey or col.isSurrogateKey or col.isSystemCreateDate) %}
                        "TGT"."{{ col.name }}" = "SRC"."{{ col.name }}"
                        {% if not loop.last %}, {% endif %}
                    {% endfor -%}
                    WHEN NOT MATCHED THEN INSERT (
                    {%- for col in source.columns if not col.isSurrogateKey %}
                        "{{ col.name }}"
                        {% if not loop.last %}, {% endif %}
                    {% endfor -%}
                    )
                    VALUES (
                    {%- for col in source.columns if not col.isSurrogateKey %}
                        "SRC"."{{ col.name }}"
                        {% if not loop.last %}, {% endif %}
                    {% endfor -%}
                    )

                {% endfor %}



                {% else %}
                    {% for source in sources %}
                        {{ stage('MERGE ' + source.name | string ) }}
                        MERGE INTO {{ ref_no_link(node.location.name, node.name) }} "TGT"
                        USING (
                            SELECT
                            {% for col in source.columns if not col.isSurrogateKey %}
                                {% if col.isSystemVersion %}
                                	1
                                {% elif col.isSystemCurrentFlag %}
                                	'Y'
                                {% else %}
                                    {{ get_source_transform(col) }}
                                {% endif %}
                                AS "{{ col.name }}"
                                {%- if not loop.last %}, {% endif %}
                            {% endfor %}
                            {{ source.join }})
                            AS "SRC"
                        ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            "SRC"."{{ col.name }}" = "TGT"."{{ col.name }}"
                        {% endfor %}
                        WHEN MATCHED
                        {% for col in source.columns if not (   col.isBusinessKey or
                                                                col.isSurrogateKey or
                                                                col.isSystemVersion or
                                                                col.isSystemCurrentFlag or
                                                                col.isSystemStartDate or
                                                                col.isSystemEndDate or
                                                                col.isSystemUpdateDate or
                                                                col.isSystemCreateDate) %}
                            {% if loop.first %}
                                AND (
                            {% else %}
                                OR
                            {% endif %}
                            NVL( CAST("SRC"."{{ col.name }}" as STRING), '**NULL**') <> NVL( CAST("TGT"."{{ col.name }}" as STRING), '**NULL**')
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                        THEN UPDATE SET
                        {%- for col in source.columns if not (  col.isBusinessKey or
                                                                col.isSurrogateKey or
                                                                col.isSystemVersion or
                                                                col.isSystemCurrentFlag or
                                                                col.isSystemStartDate or
                                                                col.isSystemEndDate or
                                                                col.isSystemCreateDate) %}
                                "TGT"."{{ col.name }}" = "SRC"."{{ col.name }}"
                            {% if not loop.last %}, {% endif %}
                        {% endfor %}
                        WHEN NOT MATCHED THEN
                        INSERT (
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            "{{ col.name }}"
                            {% if not loop.last %}, {% endif %}
                        {% endfor -%}
                        )
                        VALUES (
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            "SRC"."{{ col.name }}"
                            {% if not loop.last %}, {% endif %}
                        {% endfor -%}
                        )
                    {% endfor %}
                {% endif %}
            	
            	{% if config.postSQL %}			
            		{{ stage('Post-SQL') }}
            		{{ config.postSQL }}
            	{% endif %}
            {% endif %}

            {% if config.testsEnabled %}
            	{% for test in node.tests %}
            		{% if test.runOrder == 'After' %}
            			{{ test_stage(test.name, test.continueOnFailure) }}
            			{{ test.templateString }}
                    {% endif %}
            	{% endfor %}

            	{% for column in columns %}
            		{% for test in column.tests %}
            			{{ test_stage(column.name + ": " + test.name) }}
            			{{ test.templateString }}
            		{% endfor %}
            	{% endfor %}
            {% endif %}
    name: Dimension
    version: 1
  StepType-Fact:
    id: Fact
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |
        capitalized: Fact
        plural: Facts
        short: FCT
        tagColor: '#D9A438'

        config:
        - groupName: Options
          items:
          - type: materializationSelector
            isRequired: true
            options:
            - table
            - view
          
          - type: businessKeyColumns
            isRequired: false

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false

        systemColumns:
        - displayName: SYSTEM_CREATE_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemCreateDate

        - displayName: SYSTEM_UPDATE_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemUpdateDate
      templates:
        create:
          templateString: "

            \    {% if node.materializationType == 'table' %}

            \        {{ stage('Create Fact Table') }}

            \   \ 

            \        CREATE OR REPLACE TABLE {{
            ref_no_link(node.location.name, node.name) }}

            \        (

            \            {% for col in columns %}

            \                \"{{ col.name }}\" {{ col.dataType }}

            \                {%- if not col.nullable %} NOT NULL

            \                    {%- if col.defaultValue | length >
            0 %} DEFAULT {{ col.defaultValue }}{% endif %}

            \                {% endif %}

            \                {%- if col.description | length > 0 %}
            COMMENT '{{ col.description }}'{% endif %}

            \                {%- if not loop.last -%}, {% endif %}

            \            {% endfor %}

            \        )

            \        {%- if node.description | length > 0 %} COMMENT
            = '{{ node.description }}'{% endif %}

            \   \ 

            \   \ 

            \    {% elif node.materializationType == 'view' %}

            \        {{ stage('Create Fact View') }}

            \   \ 

            \        CREATE OR REPLACE VIEW {{
            ref_no_link(node.location.name, node.name) }}

            \        (

            \            {% for col in columns %}

            \                \"{{ col.name }}\"

            \                {%- if col.description | length > 0 %}
            COMMENT '{{ col.description }}'{% endif %}

            \                {%- if not loop.last -%},{% endif %}

            \            {% endfor %}

            \        )

            \        {%- if node.description | length > 0 %} COMMENT
            = '{{ node.description }}'{% endif %}

            \        AS

            \        {% for source in sources %}

            \   \ 

            \            {% if loop.first %}SELECT {% endif %}

            \   \ 

            \            {% for col in source.columns %}

            \                {{ get_source_transform(col) }} AS \"{{
            col.name }}\"

            \                {%- if not loop.last -%}, {% endif %}

            \            {% endfor %}

            \            {{ source.join }}

            \   \ 

            \            {% if not loop.last %} UNION ALL {% endif
            %}

            \        {% endfor %}

            \   \ 

            \    {% endif %}

            \    "
        run:
          templateString: "

            \    {% for test in node.tests if config.testsEnabled %}

            \        {% if test.runOrder == 'Before' %}

            \            {{ test_stage(test.name,
            test.continueOnFailure) }}

            \            {{ test.templateString }}

            \        {% endif %}

            \    {% endfor %}


            \    {% if node.materializationType == 'table' %}

            \        {% if config.preSQL %}

            \            {{ stage('Pre-SQL') }}

            \            {{ config.preSQL }}

            \        {% endif %}

            \       \ 

            \        {% set has_business_key = columns |
            selectattr(\"isBusinessKey\") | list | length > 0 %}

            \       \ 

            \        {% for source in sources %}

            \       \ 

            \            {% if has_business_key %}

            \       \ 

            \                {{ stage('MERGE ' + source.name |
            string ) }}

            \                MERGE INTO {{
            ref_no_link(node.location.name, node.name) }} \"TGT\"

            \                USING (

            \                    SELECT

            \                    {% for col in source.columns %}

            \                        {{ get_source_transform(col) }}
            AS \"{{ col.name }}\"

            \                        {%- if not loop.last %}, {%
            endif %}

            \                    {% endfor %}

            \                    {{ source.join }})

            \                    AS \"SRC\"

            \                ON

            \                {% for col in source.columns if
            col.isBusinessKey -%}

            \                    {% if not loop.first %}

            \                        AND

            \                    {% endif %}

            \                    \"SRC\".\"{{ col.name }}\" =
            \"TGT\".\"{{ col.name }}\"

            \                {% endfor %}

            \                WHEN MATCHED

            \                {% for col in source.columns if not
            (   col.isBusinessKey or

            \                                                        \
            col.isSystemUpdateDate or

            \                                                        \
            col.isSystemCreateDate) %}

            \                    {% if loop.first %}

            \                        AND (

            \                    {% else %}

            \                        OR

            \                    {% endif %}

            \                    NVL( CAST(\"SRC\".\"{{ col.name
            }}\" as STRING), '**NULL**') <> NVL( CAST(\"TGT\".\"{{ col.name }}\"
            as STRING), '**NULL**')

            \                    {% if loop.last %}

            \                        )

            \                    {% endif %}

            \                {% endfor %}

            \                THEN UPDATE SET

            \                {%- for col in source.columns if not
            (col.isBusinessKey or col.isSystemCreateDate) %}

            \                        \"TGT\".\"{{ col.name }}\" =
            \"SRC\".\"{{ col.name }}\"

            \                    {% if not loop.last %}, {% endif %}

            \                {% endfor %}

            \                WHEN NOT MATCHED THEN

            \                INSERT (

            \                {%- for col in source.columns if not
            col.isSurrogateKey %}

            \                    \"{{ col.name }}\"

            \                    {% if not loop.last %}, {% endif %}

            \                {% endfor -%}

            \                )

            \                VALUES (

            \                {%- for col in source.columns if not
            col.isSurrogateKey %}

            \                    \"SRC\".\"{{ col.name }}\"

            \                    {% if not loop.last %}, {% endif %}

            \                {% endfor -%}

            \                )

            \       \ 

            \            {% else %}

            \       \ 

            \                {{ stage('Insert ' + source.name |
            string ) }}

            \       \ 

            \                    INSERT INTO {{
            ref_no_link(node.location.name, node.name) }}

            \                    (

            \                        {% for col in source.columns %}

            \                            \"{{ col.name }}\"

            \                            {%- if not loop.last -%},{%
            endif %}

            \                        {% endfor %}

            \                    )

            \       \ 

            \                    SELECT

            \                    {% for col in source.columns %}

            \                        {{ get_source_transform(col) }}
            AS \"{{ col.name }}\"

            \                        {%- if not loop.last -%}, {%
            endif %}

            \                    {% endfor %}

            \                    {{ source.join }}

            \            {% endif %}

            \        {% endfor %}

            \        {% if config.postSQL %}

            \            {{ stage('Post-SQL') }}

            \            {{ config.postSQL }}

            \        {% endif %}

            \    {% endif %}


            \    {% if config.testsEnabled %}

            \        {% for test in node.tests %}

            \            {% if test.runOrder == 'After' %}

            \                {{ test_stage(test.name,
            test.continueOnFailure) }}

            \                {{ test.templateString }}

            \            {% endif %}

            \        {% endfor %}


            \        {% for column in columns %}

            \            {% for test in column.tests %}

            \                {{ test_stage(column.name + \": \" +
            test.name) }}

            \                {{ test.templateString }}

            \            {% endfor %}

            \        {% endfor %}

            \    {% endif %}

            \   \ 

            \    "
    name: Fact
    version: 1
  StepType-Hub:
    id: Hub
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |-
        capitalized: Hub
        short: 'HUB'
        plural: 'Hubs'

        tagColor: '#92712E'

        config:
        - groupName: Options
          items:
          - type: materializationSelector
            isRequired: true
            options: 
            - table
            default: table

          - displayName: Multi Source Strategy
            attributeName: insertStrategy
            type: dropdownSelector
            default: UNION
            options:
            - "UNION"
            - "UNION ALL"
            isRequired: true
            enableIf: "{% if node.isMultisource %} true {% else %} false {% endif %}"

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false

        systemColumns:
        - displayName: "LOAD_DATE"
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemCreateDate

        - displayName: "RECORD_SOURCE"
          transform: "''"
          dataType: VARCHAR
          placement: end
          attributeName: isSystemRecordSource
      templates:
        create:
          templateString: |-
            {% if node.materializationType == 'table' %}
            				{{ stage('Create Hub Table') }}
            			
            				CREATE OR REPLACE TABLE {{ ref_no_link(node.location.name, node.name) }}
            				(
            					{% for col in columns %}
            						"{{ col.name }}" {{ col.dataType }}
            						{%- if not col.nullable %} NOT NULL
            							{%- if col.defaultValue | length > 0 %} DEFAULT {{ col.defaultValue }}{% endif %}
            						{% endif %}
            						{%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
            						{%- if not loop.last -%}, {% endif %}
            					{% endfor %}
            				)
            				{%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}
            			
            			{% endif %}
        run:
          templateString: "

            \    {% for test in node.tests if config.testsEnabled %}

            \        {% if test.runOrder == 'Before' %}

            \            {{ test_stage(test.name,
            test.continueOnFailure) }}

            \            {{ test.templateString }}

            \        {% endif %}

            \    {% endfor %}


            {% if node.materializationType == 'table' %}

            \t{% if config.preSQL %}

            \t\t{{ stage('Pre-SQL') }}

            \t\t{{ config.preSQL }}

            \t{% endif %}



            \t{{ stage('Merge Hub') }}

            \tMERGE INTO {{ ref_no_link(node.location.name,
            node.name) }} \"TGT\" USING

            \t(

            \t\t{% for source in sources %}

            \t\tSELECT

            \t\t{% for col in source.columns %}

            \            {{ get_source_transform(col) }} AS \"{{
            col.name }}\"

            \t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t{% endfor %}


            \t\t{{ source.join }}


            \t\t{% if not loop.last %}

            \t\t\t{{ config.insertStrategy }}

            \t\t{% endif %}

            \t{% endfor %}

            \t)

            \tAS \"SRC\"

            \tON

            \t{% for col in sources[0].columns if
            (col.sourceColumns[0]\ 

            \                                        and
            col.sourceColumns[0].column\ 

            \                                        and
            col.sourceColumns[0].column.isHubHash

            \                                        ) -%}

            \t\t{% if not loop.first %}

            \t\t\tAND

            \t\t{% endif %}

            \t\t\"SRC\".\"{{ col.name }}\" = \"TGT\".\"{{ col.name
            }}\"

            \t{% endfor %}

            \tWHEN NOT MATCHED THEN

            \tINSERT

            \t(

            \t\t{% for col in columns %}

            \t\t\t\"{{ col.name }}\"

            \t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t{% endfor %}

            \t) VALUES

            \t(

            \t\t{% for col in columns %}

            \t\t\t\"SRC\".\"{{ col.name }}\"

            \t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t{% endfor %}

            \t)



            \t{% if config.postSQL %}

            \t\t{{ stage('Post-SQL') }}

            \t\t{{ config.postSQL }}

            \t{% endif %}

            \t

            {% endif %}


            {% if config.testsEnabled %}

            \t{% for test in node.tests %}

            \t\t{% if test.runOrder == 'After' %}

            \t\t\t{{ test_stage(test.name, test.continueOnFailure)
            }}

            \t\t\t{{ test.templateString }}

            \        {% endif %}

            \t{% endfor %}


            \t{% for column in columns %}

            \t\t{% for test in column.tests %}

            \t\t\t{{ test_stage(column.name + \": \" + test.name) }}

            \t\t\t{{ test.templateString }}

            \t\t{% endfor %}

            \t{% endfor %}

            {% endif %}

            \t\t\t"
    name: Hub
    version: 1
  StepType-Link:
    id: Link
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |-
        capitalized: Link
        short: 'LNK'
        plural: 'Links'
        tagColor: '#CA2287'

        config:
        - groupName: Options
          items:
          - type: materializationSelector
            isRequired: true
            default: table
            options:
            - table

          - displayName: Multi Source Strategy
            attributeName: insertStrategy
            type: dropdownSelector
            default: UNION
            options:
            - "UNION"
            - "UNION ALL"
            isRequired: true
            enableIf: "{% if node.isMultisource %} true {% else %} false {% endif %}"

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false

        systemColumns:
        - displayName: "LOAD_DATE"
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemCreateDate

        - displayName: "RECORD_SOURCE"
          transform: "''"
          dataType: VARCHAR
          placement: end
          attributeName: isSystemRecordSource
      templates:
        create:
          templateString: |-
            {% if node.materializationType == 'table' %}
            				{{ stage('Create Link Table') }}
            			
            				CREATE OR REPLACE TABLE {{ ref_no_link(node.location.name, node.name) }}
            				(
            					{% for col in columns %}
            						"{{ col.name }}" {{ col.dataType }}
            						{%- if not col.nullable %} NOT NULL
            							{%- if col.defaultValue | length > 0 %} DEFAULT {{ col.defaultValue }}{% endif %}
            						{% endif %}
            						{%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
            						{%- if not loop.last -%}, {% endif %}
            					{% endfor %}
            				)
            				{%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}
            			
            			{% endif %}
        run:
          templateString: "

            \    {% for test in node.tests if config.testsEnabled %}

            \        {% if test.runOrder == 'Before' %}

            \            {{ test_stage(test.name,
            test.continueOnFailure) }}

            \            {{ test.templateString }}

            \        {% endif %}

            \    {% endfor %}


            {% if node.materializationType == 'table' %}

            \t{% if config.preSQL %}

            \t\t{{ stage('Pre-SQL') }}

            \t\t{{ config.preSQL }}

            \t

            \t{% endif %}


            \t\t\t

            \t{{ stage('Merge Link') }}

            \tMERGE INTO {{ ref_no_link(node.location.name,
            node.name) }} \"TGT\" USING

            \t(

            \t\t{% for source in sources %}

            \t\tSELECT

            \t\t{% for col in source.columns %}

            \            {{ get_source_transform(col) }} AS \"{{
            col.name }}\"

            \t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t{% endfor %}


            \t\t{{ source.join }}


            \t\t{% if not loop.last %}

            \t\t\t{{ config.insertStrategy }}

            \t\t{% endif %}

            \t{% endfor %}

            \t)

            \tAS \"SRC\"

            \tON

            \t{% for col in sources[0].columns if
            (col.sourceColumns[0]\ 

            \                                        and
            col.sourceColumns[0].column\ 

            \                                        and
            col.sourceColumns[0].column.isLinkHash

            \                                        ) -%}

            \t\t{% if not loop.first %}

            \t\t\tAND

            \t\t{% endif %}

            \t\t\"SRC\".\"{{ col.name }}\" = \"TGT\".\"{{ col.name
            }}\"

            \t{% endfor %}

            \tWHEN NOT MATCHED THEN

            \tINSERT

            \t(

            \t\t{% for col in columns %}

            \t\t\t\"{{ col.name }}\"

            \t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t{% endfor %}

            \t) VALUES

            \t(

            \t\t{% for col in columns %}

            \t\t\t\"SRC\".\"{{ col.name }}\"

            \t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t{% endfor %}

            \t)


            \t{% if config.postSQL %}

            \t\t{{ stage('Post-SQL') }}

            \t\t{{ config.postSQL }}\t

            \t{% endif %}

            {% endif %}


            {% if config.testsEnabled %}

            \t{% for test in node.tests %}

            \t\t{% if test.runOrder == 'After' %}

            \t\t\t{{ test_stage(test.name, test.continueOnFailure)
            }}

            \t\t\t{{ test.templateString }}

            \        {% endif %}

            \t{% endfor %}


            \t{% for column in columns %}

            \t\t{% for test in column.tests %}

            \t\t\t{{ test_stage(column.name + \": \" + test.name) }}

            \t\t\t{{ test.templateString }}

            \t\t{% endfor %}

            \t{% endfor %}

            {% endif %}

            \t\t\t"
    name: Link
    version: 1
  StepType-Satellite:
    id: Satellite
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |-
        capitalized: Satellite
        short: 'SAT'
        plural: 'Satellites'
        tagColor: '#627DE9'

        config:
        - groupName: Options
          items:
          - type: materializationSelector
            isRequired: true
            default: table
            options:
            - table

          - displayName: Multi Source Strategy
            attributeName: insertStrategy
            type: dropdownSelector
            default: UNION
            options:
            - "UNION"
            - "UNION ALL"
            isRequired: true
            enableIf: "{% if node.isMultisource %} true {% else %} false {% endif %}"

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false

        systemColumns:
        - displayName: "LOAD_DATE"
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemCreateDate

        - displayName: "RECORD_SOURCE"
          transform: "''"
          dataType: VARCHAR
          placement: end
          attributeName: isSystemRecordSource
      templates:
        create:
          templateString: |-
            {% if node.materializationType == 'table' %}
            				{{ stage('Create Satellite Table') }}
            			
            				CREATE OR REPLACE TABLE {{ ref_no_link(node.location.name, node.name) }}
            				(
            					{% for col in columns %}
            						"{{ col.name }}" {{ col.dataType }}
            						{%- if not col.nullable %} NOT NULL
            							{%- if col.defaultValue | length > 0 %} DEFAULT {{ col.defaultValue }}{% endif %}
            						{% endif %}
            						{%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
            						{%- if not loop.last -%}, {% endif %}
            					{% endfor %}
            				)
            				{%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}
            			
            			{% endif %}
        run:
          templateString: "

            \    {% for test in node.tests if config.testsEnabled %}

            \        {% if test.runOrder == 'Before' %}

            \            {{ test_stage(test.name,
            test.continueOnFailure) }}

            \            {{ test.templateString }}

            \        {% endif %}

            \    {% endfor %}


            {% if node.materializationType == 'table' %}

            \t\t{% if config.preSQL %}

            \t\t\t{{ stage('Pre-SQL') }}

            \t\t\t{{ config.preSQL }}

            \t\t{% endif %}

            \t

            \t\t{{ stage('Merge Satellite') }}

            \t\tMERGE INTO {{ ref_no_link(node.location.name,
            node.name) }} \"TGT\" USING

            \t\t(

            \t\t\t{% for source in sources %}

            \t\t\tSELECT

            \t\t\t{% for col in source.columns %}

            \                {{ get_source_transform(col) }} AS \"{{
            col.name }}\"

            \t\t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t\t{% endfor %}

            \t

            \t\t\t{{ source.join }}

            \t

            \t\t\t{% if not loop.last %}

            \t\t\t\t{{ config.insertStrategy }}

            \t\t\t{% endif %}

            \t\t{% endfor %}

            \t\t)

            \t\tAS \"SRC\"

            \t\tON

            \t\t{% for col in sources[0].columns if
            (col.sourceColumns[0]\ 

            \                                            and
            col.sourceColumns[0].column\ 

            \                                            and
            col.sourceColumns[0].column.isChangeHash

            \                                            ) -%}

            \t\t\t{% if not loop.first %}

            \t\t\t\tAND

            \t\t\t{% endif %}

            \t\t\t\"SRC\".\"{{ col.name }}\" = \"TGT\".\"{{ col.name
            }}\"

            \t\t{% endfor %}

            \t\tWHEN NOT MATCHED THEN

            \t\tINSERT

            \t\t(

            \t\t\t{% for col in columns %}

            \t\t\t\t\"{{ col.name }}\"

            \t\t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t\t{% endfor %}

            \t\t) VALUES

            \t\t(

            \t\t\t{% for col in columns %}

            \t\t\t\t\"SRC\".\"{{ col.name }}\"

            \t\t\t\t{%- if not loop.last -%}, {% endif %}

            \t\t\t{% endfor %}

            \t\t)

            \t

            \t{% if config.postSQL %}

            \t\t{{ stage('Post-SQL') }}

            \t\t{{ config.postSQL }}\t

            \t{% endif %}

            \t

            {% endif %}


            {% if config.testsEnabled %}

            \t{% for test in node.tests %}

            \t\t{% if test.runOrder == 'After' %}

            \t\t\t{{ test_stage(test.name, test.continueOnFailure)
            }}

            \t\t\t{{ test.templateString }}

            \        {% endif %}

            \t{% endfor %}


            \t{% for column in columns %}

            \t\t{% for test in column.tests %}

            \t\t\t{{ test_stage(column.name + \": \" + test.name) }}

            \t\t\t{{ test.templateString }}

            \t\t{% endfor %}

            \t{% endfor %}

            {% endif %}

            \t\t\t"
    name: Satellite
    version: 1
  StepType-Source:
    id: Source
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |-
        capitalized: Source
        short: null
        tagColor: '#D26000'
        plural: Sources
      templates:
        create:
          templateString: |
            {{ stage('Validating Source Exists') }}
            SELECT 1 FROM {{ ref(node.location.name, node.name) }} LIMIT 0
        run:
          templateString: |
            {{ stage('Validating Source Exists') }}
            SELECT 1 FROM {{ ref(node.location.name, node.name) }} LIMIT 0
    name: Source
    version: 1
  StepType-Stage:
    id: Stage
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |
        capitalized: Stage
        short: STG
        plural: Stages
        tagColor: '#2EB67D'

        config:
        - groupName: Options
          items:
          - type: materializationSelector
            default: table
            options:
            - table
            - view
            isRequired: true

          - type: multisourceToggle
            enableIf: "{% if node.materializationType == 'table' %} true {% else %} false {% endif %}" 

          - type: overrideSQLToggle
            enableIf: "{% if node.materializationType == 'view' %} true {% else %} false {% endif %}"
            
          - displayName: Multi Source Strategy
            attributeName: insertStrategy
            type: dropdownSelector
            default: INSERT
            options:
            - "INSERT"
            - "UNION"
            - "UNION ALL"
            isRequired: true
            enableIf: "{% if node.isMultisource %} true {% else %} false {% endif %}"

          - displayName: Truncate Before
            attributeName: truncateBefore
            type: toggleButton
            default: true

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false
      templates:
        create:
          templateString: |
            {% if node.override.create.enabled %}
            	
            	{{ node.override.create.script }}

            {% elif node.materializationType == 'table' %}
            	{{ stage('Create Stage Table') }}

            	CREATE OR REPLACE TABLE {{ ref_no_link(node.location.name, node.name) }}
            	(
            		{% for col in columns %}
            			"{{ col.name }}" {{ col.dataType }}
            			{%- if not col.nullable %} NOT NULL
            				{%- if col.defaultValue | length > 0 %} DEFAULT {{ col.defaultValue }}{% endif %}
            			{% endif %}
            			{%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
            			{%- if not loop.last -%}, {% endif %}
            		{% endfor %}
            	)
            	{%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}


            {% elif node.materializationType == 'view' %}
                {{ stage('Create Stage View') }}

                CREATE OR REPLACE VIEW {{ ref_no_link(node.location.name, node.name) }}
                (
                    {% for col in columns %}
                        "{{ col.name }}"
                        {%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
                        {%- if not loop.last -%}, {% endif %}
                    {% endfor %}
                )
                {%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}
                AS
                {% for source in sources %}
                    SELECT
                    {% for col in source.columns %}
                        {{ get_source_transform(col) }} AS "{{ col.name }}"
                        {%- if not loop.last -%}, {% endif %}
                    {% endfor %}

                    {{ source.join }}

                    {% if not loop.last %}
                        {% if config.insertStrategy in ['UNION', 'UNION ALL'] %}
                            {{ config.insertStrategy }}
                        {% else %}
                            UNION
                        {% endif %}
                    {% endif %}
                {% endfor %}

            {% endif %}
        run:
          templateString: |2
            
                {% for test in node.tests if config.testsEnabled %}
                    {% if test.runOrder == 'Before' %}
                        {{ test_stage(test.name, test.continueOnFailure) }}
                        {{ test.templateString }}
                    {% endif %}
                {% endfor %}

            {% if node.materializationType == 'table' %}
            	{% if config.preSQL %}
            		{{ stage('Pre-SQL') }}
            		{{ config.preSQL }}
            	{% endif %}
            	
            	
            	
            		{% if config.truncateBefore %}
            	
            			{{ stage('Truncate Stage Table') }}
            			TRUNCATE IF EXISTS {{ ref_no_link(node.location.name, node.name) }}
            	
            		{% endif %}
            	
            	
            		{% if config.insertStrategy in ['UNION', 'UNION ALL'] %}
            			{{ stage( config.insertStrategy + ' Sources' | string ) }}
            			INSERT INTO {{ ref_no_link(node.location.name, node.name) }}
            				(
            					{% for col in columns %}
            						"{{ col.name }}"
            						{%- if not loop.last -%},{% endif %}
            					{% endfor %}
            				)
            		{% endif %}
            	
            	
            		{% for source in sources %}
            	
            			{% if config.insertStrategy == 'INSERT' %}
            				{{ stage('Insert ' + source.name | string ) }}
            	
            				INSERT INTO {{ ref_no_link(node.location.name, node.name) }}
            				(
            					{% for col in source.columns %}
            						"{{ col.name }}"
            						{%- if not loop.last -%},{% endif %}
            					{% endfor %}
            				)
            			{% endif %}
            	
            			SELECT
            			{% for col in source.columns %}
                            {{ get_source_transform(col) }} AS "{{ col.name }}"
            				{%- if not loop.last -%}, {% endif %}
            			{% endfor %}
            	
            			{{ source.join }}
            	
            			{% if config.insertStrategy in ['UNION', 'UNION ALL'] and not loop.last %}
            				{{config.insertStrategy}}
            			{% endif %}
            	
            		{% endfor %}
            	
            	{% if config.postSQL %}
            		{{ stage('Post-SQL') }}
            		{{ config.postSQL }}
            	{% endif %}
            {% endif %}

            {% if config.testsEnabled %}
            	{% for test in node.tests %}
            		{% if test.runOrder == 'After' %}
            			{{ test_stage(test.name, test.continueOnFailure) }}
            			{{ test.templateString }}
                    {% endif %}
            	{% endfor %}

            	{% for column in columns %}
            		{% for test in column.tests %}
            			{{ test_stage(column.name + ": " + test.name) }}
            			{{ test.templateString }}
            		{% endfor %}
            	{% endfor %}
            {% endif %}
    name: Stage
    version: 1
  StepType-View:
    id: View
    isDisabled: true
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |
        capitalized: View
        short: V
        tagColor: '#C4C4C4'
        isDisabled: true
        plural: Views

        config:
          - groupName: Options
            items: 
            - type: materializationSelector
              options:
                - view
              default: view
              isRequired: true

            - type: toggleButton
              attributeName: selectDistinct
              displayName: Distinct

            - type: multisourceToggle

            - type: overrideSQLToggle

            - displayName: Multi Source Strategy
              attributeName: insertStrategy
              type: dropdownSelector
              default: UNION
              options:
              - "UNION"
              - "UNION ALL" 
              isRequired: true
              enableIf: "{% if node.isMultisource %} true {% else %} false {% endif %}"
      templates:
        create:
          templateString: |
            {% if node.override.create.enabled %}
                
                {{ node.override.create.script }}

            {% else %}
                {{ stage('Create View') }}
                CREATE OR REPLACE VIEW {{ ref_no_link(node.location.name, node.name) }}
                (
                    {% for col in columns %}
                        "{{ col.name }}"
                        {%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
                        {%- if not loop.last -%}, {% endif %}
                    {% endfor %}
                )
                {%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}
                AS
                {% for source in sources %}
                    SELECT {% if config.selectDistinct %} DISTINCT {% endif %}
                    {% for col in source.columns %}
                        {{ get_source_transform(col) }} AS "{{ col.name }}"
                        {%- if not loop.last -%}, {% endif %}
                    {% endfor %}

                    {{ source.join }}

                    {% if not loop.last %}
                        {% if config.insertStrategy in ['UNION', 'UNION ALL'] %}
                            {{ config.insertStrategy }}
                        {% else %}
                            UNION
                        {% endif %}
                    {% endif %}
                {% endfor %}
            {% endif %}
        run:
          templateString: ""
    name: View
    version: 1
  StepType-persistentStage:
    id: persistentStage
    isDisabled: false
    metadata:
      defaultStorageLocation: null
      error: null
      nodeMetadataSpec: |
        capitalized: Persistent Stage
        short: PSTG
        plural: Persistent Stages
        tagColor: '#29B2DB'
            
        config:
        - groupName: Options
          items:
          - type: materializationSelector
            isRequired: true
            default: table
            options:
            - table
            - view
          
          - type: businessKeyColumns
            isRequired: false

          - type: multisourceToggle
            enableIf: "{% if node.materializationType == 'table' %} true {% else %} false {% endif %}"

          - type: overrideSQLToggle
            enableIf: "{% if node.materializationType == 'view' %} true {% else %} false {% endif %}"

          - displayName: Enable Tests
            attributeName: testsEnabled
            type: toggleButton
            default: true
            
          - displayName: Pre-SQL
            attributeName: preSQL
            type: textBox
            syntax: sql
            isRequired: false

          - displayName: Post-SQL
            attributeName: postSQL
            type: textBox
            syntax: sql
            isRequired: false

        systemColumns:

        - displayName: '{{NODE_NAME}}_KEY'
          transform: ''
          dataType: NUMBER
          placement: beginning
          attributeName: isSurrogateKey

        - displayName: SYSTEM_VERSION
          transform: ''
          dataType: NUMBER
          placement: end
          attributeName: isSystemVersion

        - displayName: SYSTEM_CURRENT_FLAG
          transform: ''
          dataType: VARCHAR
          placement: end
          attributeName: isSystemCurrentFlag

        - displayName: SYSTEM_START_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemStartDate

        - displayName: SYSTEM_END_DATE
          transform: CAST('2999-12-31 00:00:00' AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemEndDate

        - displayName: SYSTEM_CREATE_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemCreateDate

        - displayName: SYSTEM_UPDATE_DATE
          transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
          dataType: TIMESTAMP
          placement: end
          attributeName: isSystemUpdateDate
      templates:
        create:
          templateString: |
            {% if node.materializationType == 'table' %}
                {{ stage('Create Persistent Stage Table') }}

                CREATE OR REPLACE TABLE {{ ref_no_link(node.location.name, node.name) }}
                (
                    {% for col in columns %}
                        "{{ col.name }}" {{ col.dataType }}
                        {% if col.isSurrogateKey %}
            		        identity
                        {% endif %}
                        {%- if not col.nullable %} NOT NULL
                            {%- if col.defaultValue | length > 0 %} DEFAULT {{ col.defaultValue }}{% endif %}
                        {% endif %}
                        {%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
                        {%- if not loop.last -%}, {% endif %}
                    {% endfor %}
                )
                {%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}


            {% elif node.materializationType == 'view' %}
                {{ stage('Create Persistent Stage View') }}

                CREATE OR REPLACE VIEW {{ ref_no_link(node.location.name, node.name) }}
                (
                    {% for col in columns %}
                        "{{ col.name }}"
                        {%- if col.description | length > 0 %} COMMENT '{{ col.description }}'{% endif %}
                        {%- if not loop.last -%},{% endif %}
                    {% endfor %}
                )
                {%- if node.description | length > 0 %} COMMENT = '{{ node.description }}'{% endif %}
                AS
                {% for source in sources %}

            		{% if not loop.last %} UNION ALL {% endif %}
            	{% endfor %}

                    {% for col in source.columns %}
                        {% if col.isSurrogateKey or col.isSystemUpdateDate or col.isSystemCreateDate %}
                            NULL
                        {% else %}
                            {{ get_source_transform(col) }}
                        {% endif %}
                        AS "{{ col.name }}"
                        {%- if not loop.last -%}, {% endif %}
                    {% endfor %}
                    {{ source.join }}

            {% endif %}
        run:
          templateString: |-
            
            {% set has_business_key = columns | selectattr("isBusinessKey") | list | length > 0 %}
            {% set is_type_2 = columns | selectattr("isChangeTracking") | list | length > 0 %}

                {% for test in node.tests if config.testsEnabled %}
                    {% if test.runOrder == 'Before' %}
                        {{ test_stage(test.name, test.continueOnFailure) }}
                        {{ test.templateString }}
                    {% endif %}
                {% endfor %}

            {% if node.materializationType == 'table' %}
            	{% if config.preSQL %}
            		{{ stage('Pre-SQL') }}
            		{{ config.preSQL }}
            	{% endif %}
            	
                {% if has_business_key and is_type_2 %}

                    {% for source in sources %}
                        {{ stage('MERGE ' + source.name | string) }}
                        MERGE INTO {{ ref_no_link(node.location.name, node.name) }} "TGT"
                        USING (
                        /* New Rows That Don't Exist */
                        SELECT
                        {% for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemVersion %}
                                1
                            {% elif col.isSystemCurrentFlag %}
                                'Y'
                            {% else %}
                               {{ get_source_transform(col) }}
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor %}
                            'INSERT_INITAL_VERSION_ROWS' AS "DML_OPERATION"
                        {{ source.join }}
                        LEFT JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                                {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            "DIM"."{{ col.name }}" IS NULL
                        {% endfor %}
                        UNION ALL
                        /* New Row Needing To Be Inserted Due To Type-2 Column Changes */
                        SELECT
                        {% for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemVersion %}
                                "DIM"."{{ col.name }}" + 1
                            {% elif col.isSystemCurrentFlag %}
                                'Y'
                            {% else %}
                               {{ get_source_transform(col) }}
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor %}
                            'INSERT_NEW_VERSION_ROWS' AS "DML_OPERATION"
                        {{ source.join }}
                        INNER JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE "DIM"."{{ get_value_by_column_attribute("isSystemCurrentFlag") }}" = 'Y'
                        {% for col in source.columns if (col.isChangeTracking == true) %}
                            {% if loop.first %}
                                AND (
                            {% else %}
                                OR
                            {% endif %}
                            ( NVL( CAST({{ get_source_transform(col) }} as STRING), '**NULL**') <> NVL( CAST("DIM"."{{ col.name }}" as STRING), '**NULL**') )
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                        UNION ALL
                        /* Rows Needing To Be Expired Due To Type-2 Column Changes
                        In this case, only two columns are merged (version and end date) */
                        SELECT
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemEndDate %}
                                DATEADD(MILLISECONDS, -1, CAST(CURRENT_TIMESTAMP AS TIMESTAMP))
                            {% elif col.isSystemCurrentFlag %}
                                'N'
                            {% else %}
                                "DIM"."{{ col.name }}"
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor -%}
                            'update_expired_version_rows' AS "DML_OPERATION"
                        {{ source.join }}
                        INNER JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE "DIM"."{{ get_value_by_column_attribute("isSystemCurrentFlag") }}" = 'Y'
                        {% for col in source.columns if (col.isChangeTracking == true) %}
                            {% if loop.first %}
                                AND (
                            {% else %}
                                OR
                            {% endif %}
                            ( NVL( CAST({{ get_source_transform(col) }} as STRING), '**NULL**') <> NVL( CAST("DIM"."{{ col.name }}" as STRING), '**NULL**') )
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                        UNION ALL
                        /* Rows Needing To Be Updated Due To Changes To Non-Type-2 source.columns
                        This case merges only when there are changes in non-type-2 column updates, but no changes in type-2 columns*/
                        SELECT
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            {% if col.isSystemVersion or col.isSystemCreateDate or col.isSystemStartDate or col.isSystemEndDate %}
                                "DIM"."{{ col.name }}"
                            {% elif col.isSystemCurrentFlag %}
                                'Y'
                            {% else %}
                                {{ get_source_transform(col) }}
                            {% endif %}
                            AS "{{ col.name }}",
                        {% endfor -%}
                            'UPDATE_NON_TYPE2_ROWS' AS "DML_OPERATION"
                        {{ source.join }}
                        INNER JOIN {{ ref_no_link(node.location.name, node.name) }} "DIM" ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %}
                        WHERE "DIM"."{{ get_value_by_column_attribute("isSystemCurrentFlag") }}" = 'Y'
                        AND (
                        {% for col in source.columns if (col.isChangeTracking) -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            {{ get_source_transform(col) }} = "DIM"."{{ col.name }}"
                        {% endfor %} )
                        {% for col in source.columns if not (   col.isBusinessKey or
                                                                col.isChangeTracking or
                                                                col.isSurrogateKey or
                                                                col.isSystemVersion or
                                                                col.isSystemCurrentFlag or
                                                                col.isSystemStartDate or
                                                                col.isSystemEndDate or
                                                                col.isSystemUpdateDate or
                                                                col.isSystemCreateDate) -%}
                            {% if loop.first %}
                                AND (
                            {% endif %}
                            {% if not loop.first %}
                                OR
                            {% endif %}
                            NVL( CAST({{ get_source_transform(col) }} as STRING), '**NULL**') <> NVL( CAST("DIM"."{{ col.name }}" as STRING), '**NULL**')
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                    ) AS "SRC"
                    ON
                    {% for col in source.columns if col.isBusinessKey -%}
                        {% if not loop.first %}
                            AND
                        {% endif %}
                        "TGT"."{{ col.name }}" = "SRC"."{{ col.name }}"
                    {% endfor %}
                    AND "TGT"."{{ get_value_by_column_attribute("isSystemVersion") }}" = "SRC"."{{ get_value_by_column_attribute("isSystemVersion") }}"
                    WHEN MATCHED THEN UPDATE SET
                    {%- for col in source.columns if not (col.isBusinessKey or col.isSurrogateKey or col.isSystemCreateDate) %}
                        "TGT"."{{ col.name }}" = "SRC"."{{ col.name }}"
                        {% if not loop.last %}, {% endif %}
                    {% endfor -%}
                    WHEN NOT MATCHED THEN INSERT (
                    {%- for col in source.columns if not col.isSurrogateKey %}
                        "{{ col.name }}"
                        {% if not loop.last %}, {% endif %}
                    {% endfor -%}
                    )
                    VALUES (
                    {%- for col in source.columns if not col.isSurrogateKey %}
                        "SRC"."{{ col.name }}"
                        {% if not loop.last %}, {% endif %}
                    {% endfor -%}
                    )

                {% endfor %}

                {% elif has_business_key and not is_type_2 %}
                    {% for source in sources %}
                        {{ stage('MERGE ' + source.name | string ) }}
                        MERGE INTO {{ ref_no_link(node.location.name, node.name) }} "TGT"
                        USING (
                            SELECT
                            {% for col in source.columns if not col.isSurrogateKey %}
                                {% if col.isSystemVersion %}
                                	1
                                {% elif col.isSystemCurrentFlag %}
                                	'Y'
                                {% else %}
                                    {{ get_source_transform(col) }}
                                {% endif %}
                                AS "{{ col.name }}"
                                {%- if not loop.last %}, {% endif %}
                            {% endfor %}
                            {{ source.join }})
                            AS "SRC"
                        ON
                        {% for col in source.columns if col.isBusinessKey -%}
                            {% if not loop.first %}
                                AND
                            {% endif %}
                            "SRC"."{{ col.name }}" = "TGT"."{{ col.name }}"
                        {% endfor %}
                        WHEN MATCHED
                        {% for col in source.columns if not (   col.isBusinessKey or
                                                                col.isSurrogateKey or
                                                                col.isSystemVersion or
                                                                col.isSystemCurrentFlag or
                                                                col.isSystemStartDate or
                                                                col.isSystemEndDate or
                                                                col.isSystemUpdateDate or
                                                                col.isSystemCreateDate) %}
                            {% if loop.first %}
                                AND (
                            {% else %}
                                OR
                            {% endif %}
                            NVL( CAST("SRC"."{{ col.name }}" as STRING), '**NULL**') <> NVL( CAST("TGT"."{{ col.name }}" as STRING), '**NULL**')
                            {% if loop.last %}
                                )
                            {% endif %}
                        {% endfor %}
                        THEN UPDATE SET
                        {%- for col in source.columns if not (  col.isBusinessKey or
                                                                col.isSurrogateKey or
                                                                col.isSystemVersion or
                                                                col.isSystemCurrentFlag or
                                                                col.isSystemStartDate or
                                                                col.isSystemEndDate or
                                                                col.isSystemCreateDate) %}
                                "TGT"."{{ col.name }}" = "SRC"."{{ col.name }}"
                            {% if not loop.last %}, {% endif %}
                        {% endfor %}
                        WHEN NOT MATCHED THEN
                        INSERT (
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            "{{ col.name }}"
                            {% if not loop.last %}, {% endif %}
                        {% endfor -%}
                        )
                        VALUES (
                        {%- for col in source.columns if not col.isSurrogateKey %}
                            "SRC"."{{ col.name }}"
                            {% if not loop.last %}, {% endif %}
                        {% endfor -%}
                        )
                    {% endfor %}
                {% else %}
                    {% for source in sources %}
                        {{ stage('Insert ' + source.name | string ) }}
                        INSERT INTO {{ ref_no_link(node.location.name, node.name) }}
                        (
                            {% for col in source.columns if not col.isSurrogateKey %}
                                "{{ col.name }}"
                                {%- if not loop.last -%},{% endif %}
                            {% endfor %}
                        )

                        SELECT
                        {% for col in source.columns if not col.isSurrogateKey %}

                            {% if col.isSystemVersion %}
                                1
                            {% elif col.isSystemCurrentFlag %}
                                'Y'
                            {% else %}
                                {{ get_source_transform(col) }}
                            {% endif %}
                            AS "{{ col.name }}"
                            {%- if not loop.last -%}, {% endif %}
                            
                        {% endfor %}
                        {{ source.join }}
                    {% endfor %}            
                {% endif %}
            	
            	{% if config.postSQL %}
            		{{ stage('Post-SQL') }}
            		{{ config.postSQL }}
            	{% endif %}
            	
            {% endif %}

            {% if config.testsEnabled %}
            	{% for test in node.tests %}
            		{% if test.runOrder == 'After' %}
            			{{ test_stage(test.name, test.continueOnFailure) }}
            			{{ test.templateString }}
                    {% endif %}
            	{% endfor %}

            	{% for column in columns %}
            		{% for test in column.tests %}
            			{{ test_stage(column.name + ": " + test.name) }}
            			{{ test.templateString }}
            		{% endfor %}
            	{% endfor %}
            {% endif %}
    name: Persistent Stage
    version: 1
subgraphs:
  Subgraph-10:
    id: 10
    name: Product
    steps:
      - "25"
      - "24"
      - "38"
      - "53"
      - "54"
      - "55"
  Subgraph-11:
    id: 11
    name: ProductRef
    steps:
      - "39"
      - "16"
      - "21"
      - "22"
      - "24"
      - "29"
      - "36"
      - "38"
      - "53"
      - "54"
      - "55"
      - "56"
      - "57"
      - "58"
      - "59"
      - "62"
      - "63"
      - "64"
      - "65"
      - "68"
      - "70"
      - "72"
      - "73"
  Subgraph-4:
    id: 4
    name: Transaction
    steps:
      - "80"
      - "16"
      - "22"
      - "24"
      - "25"
      - "38"
      - "50"
      - "53"
      - "54"
      - "55"
      - "62"
      - "63"
      - "64"
      - "65"
      - "66"
      - "68"
      - "70"
      - "74"
      - "77"
      - "79"
  Subgraph-5:
    id: 5
    name: Sales
    steps:
      - "75"
      - "16"
      - "21"
      - "22"
      - "24"
      - "29"
      - "36"
      - "38"
      - "39"
      - "40"
      - "41"
      - "42"
      - "44"
      - "45"
      - "50"
      - "53"
      - "54"
      - "55"
      - "56"
      - "57"
      - "58"
      - "59"
      - "62"
      - "63"
      - "64"
      - "65"
      - "68"
      - "69"
      - "70"
      - "72"
      - "73"
      - "76"
      - "77"
  Subgraph-6:
    id: 6
    name: Channels
    steps:
      - "42"
      - "40"
      - "41"
  Subgraph-7:
    id: 7
    name: Entities
    steps:
      - "66"
      - "16"
      - "22"
      - "62"
      - "63"
      - "64"
      - "65"
      - "68"
      - "70"
      - "74"
  Subgraph-8:
    id: 8
    name: Panels
    steps:
      - "45"
      - "16"
      - "22"
      - "40"
      - "41"
      - "44"
      - "62"
      - "63"
      - "64"
      - "65"
      - "68"
      - "70"
  Subgraph-9:
    id: 9
    name: Manufactures
    steps:
      - "69"
      - "16"
      - "22"
      - "40"
      - "41"
      - "44"
      - "62"
      - "63"
      - "64"
      - "65"
      - "68"
      - "70"
      - "73"
version: 1
